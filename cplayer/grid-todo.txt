issues
-----------------------------------------------------
- resize horizontally with complex layouts
-

todo
-----------------------------------------------------
- hide band by dragging outside of the band area (show X at cursor)


- viewing:
		- hide/show columns (popup menu with checkboxes)
		- hide/show column header
	- vertical grid (with bands)
	- tree-list
		- expand all/collapse all option
	- per-column auto-expand to widest row option
	- double click on column splitter to expand column to widest row
- cell formatting
	- cell auto ellipsis
	- cell clipping
	- cell formatting: per-column format template or cell data type or format function
	- tooltip display on hovering over the cell or over the ellipsis part
	- rich text cells
		- multi-line
		- word wrapping
		- formatted text in cells: altering font color/shape/background, paragraphs, bullet lists, etc.
		- per-column height option
		- per-column auto-height option
		- individual cell resizing with the mouse (special drag area? hold shift? draggable inter-row line?)
- grouping:
	- group-by hierarchy
	- group-by multiple fields
	- expand/collapse all children
	- hide/show grouping header
- sorting:
	- simple column sorting
		- client-side sorting with collation
		- server-side sorting with query parmetrization
	- sort-by multiple fields (pressing shift? column groups pre-configured?)
- filtering:
	- quick column filter (like excel quick filter popup)
- searching
	- quick search over configured multiple fields
	- search bar resulting in filtering or highlighting
- selection:
	- cell select mode
	- multicell select mode
	- advanced selection: cell, row, multi-row, multi-column, constrained or free rectangle, and multi-cell select
	- invert selection
- editing:
	- immediate mode (click) or click-click mode
	- tab goes to next cell
	- right/left goes to next/prev cell
	- automatic cell advancing
	- cell advancing with tab
	- row reordering by drag&drop
	- row moving to another group via drag&drop
	- row moving to another parent via drag&drop
- cached update mode:
	- delta dataset
	- cell, row, and dataset auto-updating modes
	- save/cancel buttons
	- undo/redo log and buttons/shortcuts
	- selection dataset?
